#include<stdio.h>
int gcd(int a,int l){
	
	if(a>l)	{
		int t=a;a=l;l=t;
	}
	if(l%a==0)
	return a;
	else{
	int	r=l%a;
	return	gcd(r,a);
	}
}
int main(){
	int a,l;
	printf("ENTER THE NUMBERS:");
	scanf("%d %d",&a,&l);
	//int r=l%a;
	printf("gcd=%d",gcd(a,l));
}

