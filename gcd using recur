#include<stdio.h>
int gcd(int a,int l){
	
	if(l%a==0)
	return a;
	else{
	int	r=l%a;
		gcd(r,a);
	}
}
int main(){
	int a,l;
	printf("ENTER THE NUMBERS:");
	scanf("%d %d",&a,&l);
	int r=l%a;
	printf("gcd=%d",r);
}

output:
8
12
gcd=4
